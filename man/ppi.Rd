% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ppi.R
\name{ppi}
\alias{ppi}
\title{Prior Probability Interval for a Normal Distribution}
\usage{
ppi(l, u, interval = 0.95, annualized_growthrate = FALSE, freq = 4)
}
\arguments{
\item{l}{Numeric. The lower bound of the prior probability interval.}

\item{u}{Numeric. The upper bound of the prior probability interval.}

\item{interval}{Numeric. The prior probability mass within the interval.
Default \code{0.95}.}

\item{annualized_growthrate}{Logical. If \code{TRUE}, converts the interval
from annualized to per-period units by dividing by \code{freq}. Default
\code{FALSE}.}

\item{freq}{Integer. The data frequency (e.g. \code{4} for quarterly).
Only used when \code{annualized_growthrate = TRUE}. Default \code{4}.}
}
\value{
A list with two elements: \code{mean} and \code{var}, giving the
mean and variance of the implied normal distribution.
}
\description{
Converts a symmetric prior probability interval into the corresponding
mean and variance of a normal distribution. Given a lower and upper bound
that define a prior probability interval, this function recovers the
implied normal prior parameters. Useful for specifying informative priors
on the steady-state (Psi) parameters in an intuitive way.
}
\examples{
# steady-state annualized inflation is between 1\% and 3\% with 95\% probability
ppi(l = 1, u = 3, interval = 0.95)
}
